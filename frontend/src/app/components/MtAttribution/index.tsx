import * as React from 'react';
import Layout from 'antd/lib/layout';
import 'antd/lib/layout/style/css';

export class MtAttribution extends React.Component<{}> {
    public render() {
       return (
                <Layout className="mtAttribution">
                </Layout>
        )
    }
}
